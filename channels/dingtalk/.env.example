# DingTalk (钉钉) channel: app credentials from open.dingtalk.com → Client ID / Client Secret.
# Core connection: use channels/.env (core_host, core_port, CORE_API_KEY).
# Copy to .env and set values (or set in channels/.env).
DINGTALK_CLIENT_ID=
DINGTALK_CLIENT_SECRET=
