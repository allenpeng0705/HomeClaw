# Email channel: IMAP/SMTP account is configured in Core (config/user.yml or email account config).
# Core connection: use channels/.env (core_host, core_port, CORE_API_KEY).
# Copy to .env and add any channel-specific overrides if needed.
