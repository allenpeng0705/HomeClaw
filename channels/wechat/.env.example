# WeChat channel: host/port are in config.yml. Optional overrides here.
# Core connection: use channels/.env (core_host, core_port, CORE_API_KEY).
# Copy to .env and add any env vars your WeChat integration needs.
