# WhatsApp (neonize) channel: uses local db and config. Optional overrides here.
# Core connection: use channels/.env (core_host, core_port, CORE_API_KEY).
# Copy to .env and add any env vars your WhatsApp client needs (e.g. pairing/session).
