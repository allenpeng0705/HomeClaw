# Copy to instance_identity.yml (same folder) to identify this Core for multi-instance pairing.
# Workspace IDENTITY.md is LLM persona only; this file is machine-oriented identity.
# Testing for Mac
# instance_id: "AllenPeng-mac-HomeClaw"
# display_name: "AP-Mac-HomwClaw"
# capabilities:
#   - chat
#   - vision
# # Public URL other instances use in peers.yml (HTTPS recommended behind Tailscale/reverse proxy).
# public_base_url: "https://mymac.gpt4people.online:9000"
# # user_id peers use when calling THIS Core's POST /inbound (must exist in user.yml). Sent on invite-accept for symmetric recipient_import_peer.
# pairing_inbound_user_id: "allenpeng_mac_homeclaw"
# version_hint: "HomeClaw v0.1.0 Mac"

# Testing for Windows
instance_id: "AllenPeng-win-HomeClaw"
display_name: "AP-Win-HomwClaw"
capabilities:
  - chat
  - vision
# Public URL other instances use in peers.yml (HTTPS recommended behind Tailscale/reverse proxy).
public_base_url: "https://homeclaw.gpt4people.online:9000"
# user_id peers use when calling THIS Core's POST /inbound (must exist in user.yml). Sent on invite-accept for symmetric recipient_import_peer.
pairing_inbound_user_id: "allenpeng_win_homeclaw"
version_hint: "HomeClaw v0.1.0 Windows"
