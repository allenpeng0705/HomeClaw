# Cursor Bridge: HTTP server so HomeClaw can run commands / open files on the dev machine (where Cursor runs).
# Run: python -m external_plugins.cursor_bridge.server
# Bundled memex MCP (optional): python -m external_plugins.cursor_bridge.memex_mcp — see docs/memex-with-cursor-and-claude.md
