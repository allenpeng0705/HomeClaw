description: "This plugin generates motivational and uplifting quotes based on chat histories using a language model (LLM). Use when the user wants a quote or emotional support. Scheduling (e.g. daily quote) is done via cron_schedule with task_type run_plugin and plugin_id quotes."
