[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
# Suppress DeprecationWarning from litellm (e.g. open_text -> files() in get_model_cost_map.py)
filterwarnings =
    ignore::DeprecationWarning:litellm
