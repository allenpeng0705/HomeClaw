# Version lower bounds reduce pip resolution depth and avoid resolution-too-deep (see https://pip.pypa.io/en/stable/topics/dependency-resolution/)
# Core + Portal: all below are used by default. Optional features (plugins, channels, tools) have inline comments—comment out if not needed.
loguru>=0.7.0
PyYAML>=6.0
ruamel.yaml>=0.18.0   # preserve comments when updating core.yml
python-dotenv>=1.0.0
tqdm>=4.65.0
# transformers not required (we use llama.cpp); cognee/chromadb may pull it in optionally
chromadb>=0.4.0
numpy>=1.24.0,<3.0.0
fastapi>=0.100.0
python-multipart>=0.0.6   # Form data for Portal login/setup
uvicorn>=0.20.0   # run Portal (python -m main portal) and Core
openai>=1.0.0
litellm>=1.82.0   # use newest (Ollama/system-role fixes, Cognee cognify compatibility; see MemorySystemSummary §9)
watchdog>=3.0.0
alembic>=1.12.0
schedule>=1.2.0
croniter>=1.4.0
pytz>=2023.3
httpx>=0.24.0
# Core HTTP / templates (used by core, base, memory, channels)
aiohttp>=3.8.0
requests>=2.28.0
jinja2>=3.1.0
# DB: SQLite via sqlalchemy (memory/chat, memory/database)
sqlalchemy>=2.0.0
# User/friends store: document DB (replaces user.yml for persistence). See docs_design/UserDataTinyDB.md.
tinydb>=4.8.0
# Pydantic (models; fastapi brings it, but explicit for non-FastAPI paths)
pydantic>=2.0.0
# PPT Maker plugin (generate .pptx). Pinned to avoid resolver backtracking.
python-pptx==0.6.21
# datasets (cognee/chromadb dep) requires fsspec[http]<=2024.12.0. Pin to reduce backtracking.
fsspec[http]==2024.12.0
# Full browser: use homeclaw-browser Node.js plugin (default). For built-in Python browser tools, optional: pip install playwright && python -m playwright install chromium
# playwright>=1.40.0
# Pinggy tunnel (remote access): when pinggy.token set in core.yml, Core starts tunnel and serves /pinggy with QR for Companion.
# Install manually if you use Pinggy (many PyPI mirrors do not list it): pip install pinggy -i https://pypi.org/simple
# pinggy
# QR code for /pinggy page (Companion scan-to-connect). Required for the QR code on GET /pinggy (works with core_public_url or with pinggy tunnel).
qrcode[pil]==7.4.0

# For Channels
# Wechat
#wcferry==39.2.3.0
# whatsapp
#neonize==0.3.10
# Matrix it'scol
#simplematrixbotlib==2.12.3


# Memory: Cognee is default (memory_backend=cognee). Alternative: chroma + optional graph (kuzu/neo4j).
# Cognee is vendored in vendor/cognee. Install only its deps: pip install -r requirements-cognee-deps.txt (install.sh/install.ps1 do this). Do not pip install cognee.
# Used by Cognee (graph + robots). Stricter bounds to reduce pip backtracking.
kuzu>=0.11.3,<0.12   # graph for memory_backend=chroma / Cognee
protego>=0.1.16       # used by Cognee
# neo4j>=5.0.0   # optional: graph for memory_backend=chroma (uncomment if using Neo4j)

# Hybrid router Layer 2: semantic routing (mix mode). Uses existing embedding via custom encoder.
semantic-router>=0.0.72

# Optional: push notifications so Core can send reminders when Companion app is killed/background.
# APNs (iOS/Apple): pyjwt cryptography; FCM (Android): firebase-admin (may not work in China; iOS/macOS use APNs only, no Firebase).
firebase-admin>=6.0.0
# pyjwt[crypto]>=2.0.0   # uncomment for APNs (iOS/macOS push)

# Optional (uncomment if needed):
# document_read tool: PDF, PPT, Word, MD, HTML, XML, JSON, etc. Install via:
#   pip install -r requirements-document.txt   (install.sh/install.ps1 do this after main deps)
# web_search: provider=duckduckgo (no key) or fallback when other providers fail
duckduckgo-search>=4.0.0
# web_extract / web_crawl: better main-content extraction (free; no API key)
# trafilatura   # preferred for article extraction
# beautifulsoup4 # fallback if trafilatura not installed
# pypdf   # fallback for PDF only if unstructured not installed
# llama-cpp-python   # in-process LLM via LlamaCppPython (default is llama.cpp server subprocess or litellm)
# simplematrixbotlib # Matrix channel (channels/matrix)
# torch optional: CUDA detection uses nvidia-smi when torch not installed
# MCP client (optional): for mcp_call and mcp_list_tools to use external MCP servers. See docs/mcp.md.
mcp
# Tests (optional: install only for development)
pytest>=7.0.0
pytest-asyncio>=0.21.0
