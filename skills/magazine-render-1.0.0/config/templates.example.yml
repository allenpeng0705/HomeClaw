# DEPRECATED — config/templates.yml is not used by render_magazine.py
#
# The skill builds AST from Python functions (_daily_brief_magazine_ast, etc.)
# and does not read a templates.yml file at render time.
#
# Keeping this file in place as a reminder:
# If you want user-editable templates in a future version, wire them up via
# render_magazine.py's argument parser (--template-name flag → load from YAML).
#
# For now, the only customization paths are:
# - --theme dispatch|minimal (built into the Python AST builders)
# - --document-layout digest_table|magazine|newspaper (daily brief path)
# - --chrome-template <path> (AST chrome overlay for daily-brief magazine mode)